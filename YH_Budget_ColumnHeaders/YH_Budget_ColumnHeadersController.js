({

	scrollHeaders : function(cmp, event, helper) {
		helper.scrollHeaders(cmp, event);
	},
	
	toggleHeader: function(cmp, event, helper) {
		helper.toggleHeader(cmp, event);
	},
	
	handleYears: function(cmp, event, helper) {
		helper.handleYears(cmp, event);
	}
	
})